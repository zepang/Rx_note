var arr = [21,4,6,9,19,28,54,32,15,48]
function sort (arr) {
  var middle = parseInt(arr.length/2, 10)
  var i,j,tmp, result

  for (i = 0;i < arr.length; i++) {
    var tmp = arr[i]
    j = middle + i
   while (j < arr.length && tmp > arr[j]) {
      
    }
  }
}